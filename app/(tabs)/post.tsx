import React from 'react';
import { Text, View } from 'react-native';

type Props = {
  navigation: any;
  route: any;
};

const post = (props: Props) => {
  return (
    <View>
      <Text>post</Text>
    </View>
  );
};

export default post;
